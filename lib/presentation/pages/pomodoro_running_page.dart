import 'package:flutter/material.dart';

class PomodoroRunningPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      child: Text('POMODORO RUNNING PAGE'),
    );
  }
}
